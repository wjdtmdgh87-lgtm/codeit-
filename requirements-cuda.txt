# NVIDIA GPU (CUDA) 사용자용 추가 설치
# pip install -r requirements.txt
# pip install -r requirements-cuda.txt

--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.11.0+cu128
torchvision==0.26.0+cu128
